
export function add(number1, number2) {
  sum = number1 + number2;
}
function sum(number, numer2, number3) {
  return number + numer2 + number3;
}

const addTwo = (num, num2) => {
  return num + num2;
};
const name = () => 2 + 2

console.log("Sum of Numbers", name());
// console.log("Sum of Numbers", addTwo(2, 3));

export default sum;
export { addTwo }